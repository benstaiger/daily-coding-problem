
# This problem was asked by Microsoft.
#
# Given an array of numbers and a number k, determine if there are three
# entries in the array which add up to the specified number k. For example,
# given [20, 303, 3, 4, 25] and k = 49, return true as 20 + 4 + 25 = 49.


def find_sum_3(numbers, total):
    """
    """
    # Brute force requires O(N^3)
    # Can we do this in linear time from a sorted List?
    # We start one pointer at the end and one at the beginning
    # if we need a smaller total we move the end pointer back
    # if we need a larger total, we move the beginning pointer forward
    # [1, 3, 4, 20, 25, 48, 303], 49
    # we can adapt that type of 2 pointer approach to a 3-pointer
    # by having a pointer starting at the end then doing the 2 pointer on the
    # remaining array looking for K - p3




def test_find_sum_3():
    assert find_sum_3([20, 303, 3, 4, 25], 49)
